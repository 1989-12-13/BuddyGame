# 2026-07-12

## UI Dark Theme Overhaul (ui branch)

Based on two review docs (`.context/ui打磨guide-minimax.md` and `.context/游戏评价-glm.md`), performed a comprehensive dark dispatch console UI overhaul.

### Changes
- **tokens.css**: Replaced light palette with dark dispatch console (OLED black #0a0e14, terminal green #00ff88, alert red #ff3b3b, phosphor amber #ffb000)
- **Fonts**: Added IBM Plex Mono (numbers/terminal) + Inter (body) via Google Fonts in index.html
- **TitleScreen**: Dark terminal aesthetic with animated ECG waveform, live clock status bar, corner brackets, scanline overlay
- **Hud**: Replaced all emoji (⏱📞📋⭐🚑) with inline SVG icons, monospace values, terminal colors
- **EndingScreen**: Per-call score cards with save/miss indicators (◆救回/◇错失), "救回 N/5人" summary, rating-based badge. Added callScores prop from App→EndingScreen
- **GameScreen**: Systematic sed replacement of 155+ hex colors to dark palette. Replaced emoji with Unicode symbols
- **All other screens**: LevelSelectScreen, KnowledgeScreen, CallDebrief, minigames all converted to dark theme
- **types.ts**: Updated MPDS_DETERMINANT_INFO and STRESS_INFO colors for dark theme, replaced emoji
- **animations.css**: Added ECG scroll/glow, scanline sweep, call-incoming pulse, card-reveal animations

### Key Decisions
- Used sed for bulk color replacement (efficient for 155+ occurrences in GameScreen)
- Kept #fff for button text on colored backgrounds (red/amber buttons)
- Kept skin tone colors (#fadcbc, #c9a98b) in minigame body diagrams
- Used Python for reliable emoji replacement (sed had issues with multi-byte chars on Windows)
- Commit: `2ea4381` on `ui` branch

### Git
- Working on `ui` branch — all commits go here, not main

## Lucide React Icon Migration
- Installed `lucide-react`, replaced all inline SVG / unicode icons in Hud, TitleScreen, EndingScreen
- Hud: Clock, Phone, List, Star, Truck
- TitleScreen: Play, Map, BookOpen
- EndingScreen: Trophy, ShieldCheck, ShieldAlert, Skull, RotateCcw
- JS bundle: ~717KB → ~725KB (+8KB from tree-shaken lucide)
- Commit: `b378943`

## Restore Red Phone Icon
- Master had `📞` at CallWaiting (64px pulsing), AskBtnEx, and FieldRow
- Replaced with Lucide `<Phone>`: 64px red at CallWaiting, 10-13px in buttons/forms
- Refactored AskBtnEx/FieldRow/SectionTitle/DetailItem `icon` prop from `string` to `ReactNode`
- Commit: `41b26d9`
