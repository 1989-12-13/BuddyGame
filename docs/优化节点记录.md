# 《120 调度台》优化节点记录

更新日期：2026-09-10

## 节点 0：环境基线

- 已执行 `codegraph init -i`，项目索引建立完成；`.codegraph/`、`.cursor/` 与 `.vscode/` 均不纳入版本控制。
- 开始前保留工作区已有的小游戏、Chap3、体征条、配色和 E2E 修改，没有用旧提交整体覆盖文件。
- 基线检查：类型检查、Lint、924 个 Vitest、10 个 Playwright E2E 和生产构建全部通过。

## 节点 1：恢复完整登记表

- 参考 `df42c753fbf962189d4fd34986530b5cdd9f88da` 的信息顺序，在当前三栏工作台中直接展示完整 `TerminalForm`。
- 患者体征保持为独立摘要；地址、电话、主诉、年龄、性别、意识、呼吸、协议、判定码、分诊、子编码和备注按登记顺序展开。
- 派车资格与 reducer 共用同一个校验结果，底部持续显示缺失项。
- 节点检查：`npm run typecheck` 通过；工作台 1920×1080、1366×768、1280×720、1024×768、390×844 六项 E2E 通过。

## 节点 2：后台救援

- 挂断已派车来电时保存车辆、路线、患者、指导和通话实例快照。
- 救援随统一时钟继续推进，到达后只结算一次，并把结果写回通话历史和通知；最终一通存在后台任务时不会提前进入班次总结。
- 检查点版本升级到 v2，并兼容读取 v1；刷新后可恢复后台车辆、任务、历史和待确认通知。
- 节点检查：`session.test.ts` 覆盖挂断、重复 Tick、单次结算和检查点恢复，通过。

## 节点 3：派车、评分与能力

- 死亡或已经结算的患者不能再次派车；无车与缺失信息返回明确中文原因。
- 有小游戏的指导按判断 60%、小游戏 40% 计算；参加后得到 0 分会按 0 分结算。
- `field_first_aid` 抵消第一次错误；`priority_channel` 在路线节奏计算后再减 5 秒；`protocol_hint` 每通突出一个未确认必要事实。
- 节点检查：`dispatchPlanning.test.ts`、`worldState.test.ts`、`rescueResolution.test.ts` 通过。

## 节点 4：二次问询

- 模糊回答可在安抚降低压力后再确认一次；清楚答案保持锁定。
- 重问不会重复写入已问列表、判断题或得分，第三次提交无效。
- 节点检查：`session.test.ts` 覆盖“模糊地址 → 安抚 → 再确认”和防刷分，通过。

## 节点 5：途中改道

- 脑卒中主线在预定路况变化后显示一次改道决策，可保持当前路线或改走 ETA 更低的备选路线。
- 改道动作绑定通话实例；旧通话、过期选择和第二次改道均无效。
- 节点检查：reducer 回归通过；Playwright 完成“路况更新 → 改道 → 到达 → 交接”真实界面流程。

## 节点 6：现场交接

- 救护车到达后，从本通已确认的地址、意识、呼吸、主诉、指导和路线中选择 3–5 项真实信息。
- 第一次漏项会指出遗漏和不应确定的内容；第二次允许修正；重复提交无效。
- 提前挂断记录为 `missed-handoff`，但救护车仍继续执行任务。
- 节点检查：单元测试覆盖未知信息、错误首交和二次修正；浏览器 E2E 覆盖正确交接。

## 节点 7：指导小游戏

- 心肺复苏节奏、止血步骤排序和人工呼吸均写入同一指导完成度与风险模型。
- 快速连点不能越过 Chap3 反馈确认；切换步骤时停止旧语音；旧通话和非法分数提交会被拒绝。
- 等待车辆期间提供五个分时照护检查和一个场景专项互动，复习不重复加分。
- 节点检查：小游戏、指导 reducer、快速连点和语音衔接用例通过。

## 节点 8：UI 与患者体征

- 统一工作台、登记表、小游戏、改道和交接的深蓝灰/温白/低饱和青绿色；危险用红色，待确认用琥珀色。
- 体征条明确标为“模拟照护余量”，增加持续消耗/现场接手趋势，避免被误读为血氧、心率或存活概率。
- 小屏幕下改道和交接改为单列；登记表控件补齐可访问名称，按钮保持至少 44px。
- 节点检查：明暗主题对比度、键盘焦点、390px 触屏宽度和无横向溢出 E2E 覆盖。

## 节点 9：节奏与内容

- 五通固定主线的等待事件密度提升；清理结局中没有记录依据的诊断、概率和院后结果。
- 复盘分开呈现操作评价与模拟救援结局，并明确显示指导完成度和现场交接状态。
- 自动脚本有效游玩时间为 1128 秒（18 分 48 秒），位于 15–20 分钟目标内；暂停、复盘和车辆周转不计入。
- 该时长属于自动流程验证，仍需团队按计划邀请至少 5 名无急救背景玩家完成线下试玩并记录真实时长与卡点。

## 节点 10：完整验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test -- --run`：23 个测试文件、936 个测试全部通过。
- `npm run test:e2e`：11 个浏览器流程全部通过。
- `npm run build`：通过，Vite 成功构建 2392 个模块。
- 急救文字、图示和操作仍需团队安排具备资质的急救专业人员复核；自动测试不构成医学认证。
